services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: grassguy-cloudflared
    restart: unless-stopped
    command: tunnel --no-autoupdate run --token ${CLOUDFLARE_TUNNEL_TOKEN}
    depends_on:
      - grassguy-football-designer

